
- build_utils.py: tools to build CSVs
- source_comparison_utils.py: tools to compare two sources statistically and make plots
- analysis_by_source.ipynb: notebook to compare two sources (uses tools from sources_comparison_utils.py)

TODO: (Dec 2)
Add pipeline for per map statistical analysis and plots; utils.py file and notebook.
My old notebook from October displays completion times distributions for a given map (visualize_completion_times_25-10.ipynb- TO BE REMOVED), using final completion times in old data format (nested dictionaries; to become outdate onced I update the code). 
I also need to use state_histories (and possibly collision data), to investigate maps a bit more. (Although prioririty might be higher-level understanding, tbd)
((This new pipeline might be most useful for our understanding; not sure it will help with final paper outputs, although it's always nice to have a pipeline for finer analysis))

Old files in old_notebooks. Including sim_analysis.py and draft_analysis.ipynb (inside notebooks-01-12-2025), which have tools to convert files from old to new format.